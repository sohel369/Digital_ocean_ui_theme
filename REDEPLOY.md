# 🚀 Railway Redeploy & Auth Fix Guide

রেলওয়েতে "Not authenticated" বা "500 Error" এর সমস্যা সমাধানের জন্য নিচের ধাপগুলো অনুসরণ করুন:

### ১. কোড আপডেট (Push Changes)
আমি ব্যাকএন্ডের `main.py` এর সিনট্যাক্স এরর ফিক্স করেছি এবং একটি **Debug Tool** যোগ করেছি। এই পরিবর্তনগুলো রেলওয়েতে পাঠানোর জন্য Git-এ push করুন।

### ২. Railway Variables চেক করুন
Railway Dashboard-এ গিয়ে আপনার **Backend Service**-এ নিচের variables গুলো অবশ্যই সেট করুন:
*   `JWT_SECRET` = (আপনার একটি সিক্রেট কোড, যেমন: `my_super_secret_key_123`)
*   `ACCESS_TOKEN_EXPIRE_MINUTES` = `1440`
*   `DATABASE_URL` = (আপনার PostgreSQL URL - এটি সাধারণত অটোমেটিক থাকে)

### ৩. ব্রাউজার মেমোরি ক্লিন করুন (জরুরি)
পুরাতন ইনভ্যালিড টোকেন রিমুভ না করলে আপনি বারবার Error পাবেন। আপনার রেলওয়ে সাইটটি খুলে ব্রাউজার কনসোলে (F12) নিচের কোডটি রান করুন:
```javascript
localStorage.clear();
sessionStorage.clear();
location.reload();
```

### ৪. ডায়াগনস্টিক চেক (Diagnostic Check)
সবকিছু ঠিক আছে কিনা তা দেখতে নিচের লিঙ্কে আপনার রেলওয়ে URL ব্যবহার করে ভিজিট করুন:
*   `https://your-railway-url.app/api/debug/system` (সিস্টেম স্ট্যাটাস)
*   `https://your-railway-url.app/api/debug/db` (ডাটাবেস টেবিল ও কলাম চেক)
*   `https://your-railway-url.app/api/debug/env` (JWT সিক্রেট সেট হয়েছে কিনা)

### ৫. লগইন করুন
এবার নতুন করে লগইন করার চেষ্টা করুন:
*   **Email**: `admin@adplatform.com`
*   **Password**: `admin123`

---
**বি.দ্র.:** যদি তবুও সমস্যা হয়, তবে Railway Logs-এ দেখুন কোনো Python এরর আছে কিনা। আমি এরর ডিটেকশন অনেক উন্নত করে দিয়েছি।
